#include "ros/ros.h"
#include "dji_sdk/MissionWpUpload.h"


// argc : argument count, number of arguments passed to the node
// argv : argument vector, an array contaning the individual arguments passed to program. 

int main(int argc, char **argv)
{
  // Starting ros
  ros::init(argc, argv, "move_node");

  ros::NodeHandle n;

  // ros clients
  // waypoint_upload_service = n.serviceClient<dji_sdk::MissionWpUpload>("dji_sdk/mission_waypoint_upload"); 
  

  // Creating waypoint message 
  dji_sdk::MissionWaypoint waypoint;
  
}



